#include <stdio.h>
#include <stdlib.h>

int main()
{
    char letter, name;
    int num1;
    scanf("%c", &letter);
    printf(" the letter is : %c\n", letter);
    scanf("%d", &num1);
    printf("the namber is %d\n", num1);
    scanf("%c", &name);
    printf("the n is %c\n", name);
    return 0;
}